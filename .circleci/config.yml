version: 2.1

executors:
  default:
    working_directory: ~/repo
    docker:
      -image: circleci/ruby:3.1.2
      environment:
        RAILS_ENV: test

  commands:
    defaults:
      steps:
        - checkout
        - run: bundle install
jobs:
  lintering:
    executor: default
    steps:
      -defaults

  workflows:
    version: 2.1
    build:
      jobs:
        - lintering